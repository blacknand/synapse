#include "include/synapse/details/concurrent_hash_map.h"


template<typename Key, typename Value, typename Hash = std::hash<Key>>
void ConcurrentHashMap::insert(const Key& key, const Value& val)
{
    
}


template<typename Key, typename Value, typename Hash = std::hash<Key>>
void ConcurrentHashMap::get(const Key& key) const
{
    
}


template<typename Key, typename Value, typename Hash = std::hash<Key>>
void ConcurrentHashMap::erase(const Key& key) const
{
    
}